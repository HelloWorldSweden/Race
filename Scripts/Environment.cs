using UnityEngine;

public class Environment : MonoBehaviour
{
    public float halfLength = 104.5f; //z = 209
    

   
    // Start is called before the first frame update
    void Start()
    {
       
    }

    // Update is called once per frame
    void Update()
    {
    }

    
}
